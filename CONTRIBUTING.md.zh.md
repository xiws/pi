# 为 pi 贡献

本指南旨在为双方节省时间。

## 理念

首先：**pi 的核心保持精简**。

如果你的功能不属于核心，那应该实现为扩展。使核心膨胀的 PR 通常会被拒绝。

pi 的核心存在是为了保持精简，并作为可扩展的基础，让扩展能够影响并操控它。不过，扩展使用的挂钩点也必须经过认真考虑和充分讨论，以避免引入难以维护的膨胀和复杂的交互。

## 核心规则

**你必须理解你的代码。** 如果你无法解释你的改动做了什么、如何与系统中其他部分交互，你的 PR 将被关闭。

使用 AI 写代码没有问题，但提交一段自己都不理解的 AI 生成的 slop，不行。

如果需要借助 agent，请在 `pi` 根目录下运行，这样它会自动拾取 `AGENTS.md`。你的 agent 必须遵循该文件中列出的规则与指南。

## 贡献评审门

所有来自新贡献者的 Issue 和 PR 默认会被自动关闭。

周五到周日提交的 Issue 不保证会被审查。如果事情紧急，请在 Discord 上询问：https://discord.com/invite/3cU7Bz4UPx

维护者每天都会审查被自动关闭的 Issue，并重新打开其中有价值的。不符合以下质量标准的 Issue 不会被重新打开或收到回复。

维护者对 Issue 的回复将通过以下方式批准：

- `lgtmi`：你后续的 Issue 将不会被自动关闭
- `lgtm`：你后续的 Issue 和 PR 都将被放行（不会被自动关闭）

命令必须位于回复的开头（允许在 `@用户名` 提及之后）或末尾。`lgtmi` 不授予提交 PR 的权限。只有 `lgtm` 才授予提交 PR 的权限。

## Issue 质量标准

如果你要提交一个 Issue，必须使用两个 GitHub 模板之一。

如果你要提交一个 Issue，请保持简短、具体，且值得阅读。

- 保持简洁。如果内容超出单屏范围，说明太冗长了。
- 用自己的声音写（不要使用 LLM 生成文本，万不得已再用，则必须跟一条明确标注为 AI 的评论）。
- 清晰地说明 bug 或需求。
- 解释它为什么重要。
- 如果你想自己动手实现改动，请说出来。

如果 Issue 确实存在且写得不错，维护者可能会重新打开它，或在上述命令位置回复 `lgtmi` 或 `lgtm`。

## 封号

如果你两次违反本文件的精神，或者通过 agent 向 tracker 大量推送垃圾 Issue，你的 GitHub 账号将被永久封禁。

如果你通过自动化大量发送 Issue，你的 GitHub 账号将被永久封禁。没有取回的余地。

## 提交 PR 前

除非你已经通过维护者以上述方式批准的 `lgtm`，否则不要提交 PR。

提交 PR 之前：

```bash
npm run check
./test.sh
```

两者都必须通过。

不要编辑 `CHANGELOG.md`。Changelog 条目由维护者添加。

如果你要在 `packages/ai` 中添加新 provider，请参见 `AGENTS.md` 中要求的测试。

## 有问题？

请在 [Discord](https://discord.com/invite/nKXTsAcmbT) 上提问。

## 常见问题

### 为什么新的 Issue 和 PR 会被自动关闭？

pi 收到的 Issue 数量远超维护者能在实时处理的范围。许多报告不符合本指南的质量标准，也未遵循 CONTRIBUTING.md。一些则是被 agent 毫无意义地甩到仓库，而非由提交者本人审核和整理。自动关闭为创建了一个缓冲，让维护者可以按自己的时间表审查 tracker，并重新打开那些达到质量标准的 Issue。

### 为什么周末的 Issue 优先级较低？

我们在工作时间处理 tracker。这意味着周末期间会积累更多 Issue。周五到周日提交的任何内容都可能在周一审查队列中被遗漏或赋予较低优先级。如果问题紧急，请在 Discord 上提出，并附上简要描述、复现步骤和相关日志。

### 为什么有些 Issue 没有任何回复？

回复本身也是维护工作。低信号、描述不清、重复，以及未遵循本指南的 Issue 可能在不进行讨论的情况下被关闭。这为可复现的 bug、经过深思的请求，以及真正付出了劳动使报告具备可操作性的人保留了时间。

### 为什么不让 AI 来处理分发？

AI 可以帮助归类重复项、总结报告并发现缺失信息。但它不被信任来做最终的维护者决策。精心制作但由 AI 生成的 Issue 仍然可能出错、具有误导性，或调查成本高昂。人类审查仍然是最终关口。

### 这难道是对贡献者的敌意吗？

不是。这是对倦怠和 tracker 垃圾信息的护栏。简短、具体、可复现的 Issue 受到欢迎。经过深思熟虑的贡献受到欢迎。自动垃圾、 entitlement 心态和低质量的大量报告不受欢迎。

## 计划信息哪里看？

Earendil 使用 RFC 来讨论较大的改动。其中一些是公开的，可以在 [rfc.earendil.com](https://rfc.earendil.com/keyword/pi/) 上找到。
